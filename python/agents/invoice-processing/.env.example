# ============================================================
# Invoice Processing Agent — Environment Configuration
# ============================================================

# ---------------------------------------------------------------------------
# Option A: Run with Gemini API key (full LLM features)
# ---------------------------------------------------------------------------
GEMINI_API_KEY=your_key_here

# ---------------------------------------------------------------------------
# Option B: Demo mode (no API key needed)
# Set DEMO_MODE=true and leave GEMINI_API_KEY blank.
# All 8 ALF tools return realistic pre-canned responses.
# ---------------------------------------------------------------------------
DEMO_MODE=false

# ---------------------------------------------------------------------------
# Optional: Model overrides
# Defaults: gemini-2.5-flash (ADK agent), gemini-2.5-pro (ALF/learning tools)
# ---------------------------------------------------------------------------
# GEMINI_FLASH_MODEL=gemini-2.5-flash
# GEMINI_PRO_MODEL=gemini-2.5-pro

# ---------------------------------------------------------------------------
# Optional: Rate limiting (seconds between internal LLM calls)
# Default: 1.0 — increase if hitting quota limits
# ---------------------------------------------------------------------------
# API_CALL_DELAY_SECONDS=1.0
